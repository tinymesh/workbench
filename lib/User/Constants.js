import {BASE_URL} from '../Constants'

export default {
  USER_URL: BASE_URL + '/user',
  REGISTER_URL: BASE_URL + '/user/register',
  Actions: {
    update: 'user:update',
    change: 'user:change'
  }
}

